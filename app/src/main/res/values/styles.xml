<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->

        <!-- colorPrimary is the color of the appbar -->
        <item name="colorPrimary">@color/colorPrimary</item>

        <!-- colorAccent is the color of checkboxes, radio buttons -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>

        <!-- colorAccent is the color of checkboxes, radio buttons -->
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">#ffffffff</item>

        <!-- default color of radio buttons to white -->
        <item name="radioButtonStyle">@style/MyRadioButtonStyle</item>
        <item name="android:radioButtonStyle">@style/MyRadioButtonStyle</item>

        <item name="android:windowBackground">@color/activitybackground</item>

        >

    </style>

    <!-- custom style -->
    <style name="MyRadioButtonStyle" parent="Widget.AppCompat.CompoundButton.RadioButton">
        <item name="android:textColor">@color/colorText</item>
        <item name="buttonTint">@color/colorText</item>

    </style>

    <style name="MyDialogTheme" parent="Theme.AppCompat.Dialog.Alert">
        <item name="android:background">@color/colorAccent</item>

    </style>
</resources>